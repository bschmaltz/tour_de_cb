== README
This app was copied (with permission) from a private Career Builder repo, so I share it on my portfolio.

I did not create the code in the /code directory. Those two files are for calculating resistance from the desk cycle and sending the respected distance in a post request.